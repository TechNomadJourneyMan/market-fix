[tool.vercel]
entrypoint = "app.py"

[project]
name = "market-fix"
version = "0.1.0"
description = "Market Fix — AI Marketplace заведений Алматы"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "streamlit>=1.34",
    "pandas>=2.2",
    "numpy>=1.26",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.0"
]
